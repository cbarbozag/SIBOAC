@model IEnumerable<Cosevi.SIBOAC.Models.DispositivoPorRolPersona>

@{
    ViewBag.Title = "Index";   
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CodigoRolPersona)
        </th>
        <th>
            @Html.Label("Descripción RolPersona")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodigoDispositivo)
        </th>
        <th>
            @Html.Label("Descripción Dispositivo")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoRolPersona)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcionRolPersona)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoDispositivo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcionDispositivo)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { CodRol = item.CodigoRolPersona, CodDisp = item.CodigoDispositivo /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { CodRol = item.CodigoRolPersona, CodDisp = item.CodigoDispositivo  /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { CodRol = item.CodigoRolPersona, CodDisp = item.CodigoDispositivo /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
