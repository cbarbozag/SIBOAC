@model IEnumerable<Cosevi.SIBOAC.Models.GetDescargaInspectorData_Result>
@{
    ViewBag.Title = "_DescargaInspector";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>Reporte por Descarga del Inspector</h2>
<h4>Filtro</h4>
<hr />
@using (Html.BeginForm("GetReporteDescargaInspector", "Reporte", FormMethod.Get))
{
  
    <div class="form-group">
        <label class="control-label col-md-2">Rango de fechas</label>
        <div class="col-md-10">
            <div class="input-daterange input-group" id="datepicker">
                <input id="desde" type="text" class="input-sm form-control" name="desde" value="@Request.Params["desde"]" />
                <span class="input-group-addon">a</span>
                <input id="hasta" type="text" class="input-sm form-control" name="hasta" value="@Request.Params["hasta"]" />
            </div>
        </div>
    </div>

    <br />
    <br />
    <br />

    <div class="form-group">
        <br>
        Numero de la Hand Held
        <br>
        <input id="numeroHH" type='text' name='numeroHH' class="input-sm form-control" value="@Request.Params["numeroHH"]" @*size='30' maxLength='60'*@>
        <br />
       
        <br>
        <div class="form-group">
            <div class="row">
                <div class="col-md-10">
                    <div class="form-group">
                        <label class="control-label col-md-2"> Oficial de Transito</label>
                        <div class="col-max col-md-8">
                            <select name="listaInspectores" id="listaI" multiple="multiple" class="list-group" data-toggle="items">
                                @foreach (var item in ViewBag.Inspectores)
                                {

                                    if (item.Seleccionado == true)
                                    {
                                        <option value="@item.Id" selected>@item.Descripcion</option>
                                    }
                                    else
                                    {
                                        <option value="@item.Id">@item.Descripcion</option>
                                    }


                                }
                            </select>
                        </div>
                    </div>
                </div>
            </div>
            </div>
            <br>
        </div>
             <div class="pull-right">
            <input type="submit" id="reporte" value="Generar Reporte" class="btn btn-success" />
        </div>

    }

                    <iframe id="ifrmReportViewer" style="height:1200px; width:99%;" frameborder="0" scrolling="no"></iframe>
                    @section scripts
    {
                        <script>
                            $(document).ready(
                            function () {
                                Calendario();
                                ReportCall();
                            });
                        </script>

                        <script>
                            function Calendario() {
                                $('.input-daterange').datepicker({
                                    startView: 2
                                });
                            };

                            function ReportCall() {

                                var url = '/Reports/ViewerPage.aspx?reporteID=@ViewBag.ReporteID&nombreReporte=@ViewBag.NombreReporte&parametros=@ViewBag.Parametros';

                                var myFrame = document.getElementById('ifrmReportViewer');
                                if (myFrame !== null) {
                                    if (myFrame.src) {
                                        myFrame.src = url;
                                    }
                                    else if (myFrame.contentWindow !== null && myFrame.contentWindow.location !== null) {
                                        myFrame.contentWindow.location = url;
                                    }
                                    else {
                                        myFrame.setAttribute('src', url);
                                    }
                                }

                                return false;
                            }

                            $("#listaInspectores").change(function () {
                                var id = $(this).attr('name');
                                var str2 = "";
                                $("#listaInspectores option:selected").each(function () {

                                    str2 += "&" + id + "=" + $(this).attr('value');
                                });
                                $("div").text(str2);
                            })
                                   .trigger('change');
                        </script>
                    }

